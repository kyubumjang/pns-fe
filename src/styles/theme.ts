import { Theme } from '@emotion/react';

import colors from '@/constants/colors';
const theme: Theme = {
  color: colors,
  fontSize: {
    xl: '20px',
    lg: '18px',
    md: '16px',
    sm: '14px',
    xs: '12px',
  },
  borderRadius: '8px',
};

export default theme;
