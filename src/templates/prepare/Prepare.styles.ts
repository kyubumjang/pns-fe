import styled from '@emotion/styled';

export const PrepareContainer = styled('div')({
  display: 'flex',
  paddingTop: '60px',
});

export const PrepareInfoContainer = styled('div')({
  display: 'flex',
  width: '100%',
  height: '50vh',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
});

export const PrepareTitle = styled('div')({
  display: 'flex',
  flexDirection: 'column',
});

export const LinkWrapper = styled('div')({
  marginTop: '50px',
  cursor: 'pointer',
});
